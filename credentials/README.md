Place your Google OAuth files here:

- `credentials.json` — downloaded from Google Cloud Console
  (see main README > "Google OAuth 2.0 Setup Guide")
- `token.json` — auto-generated after your first successful sign-in;
  do not commit this file to version control.
